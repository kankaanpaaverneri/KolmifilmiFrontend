interface Paths {
  root: "/";
  presentation: string;
  productions: string;
  contact: string;
  notFound: "*";
}

export const paths: Paths = {
  root: "/",
  presentation: "/presentation",
  productions: "/productions",
  contact: "/contact",
  notFound: "*",
};
